apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: miniserversystem
spec:
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
