apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: miniserversystem
spec:
  template:
    spec:
      volumes:
        - name: data
          emptyDir: {}
  volumeClaimTemplates: []
